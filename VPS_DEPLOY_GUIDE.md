# 🚀 Telegram Poll Bot - VPS 1-Click Auto-Run Deployment Guide

এই গাইডটি এমনভাবে তৈরি করা হয়েছে যাতে যে কেউ (কোনো অ্যাডভান্সড টেকনিক্যাল জ্ঞান ছাড়াই) মাত্র **১টি কমান্ড** দিয়ে VPS-এ বটটি ২৪/৭ চালু করতে পারে এবং **VPS রিবুট হলেও বট যেন একা একাই চালু হয়**।

---

## 🔥 সবচেয়ে সহজ ১-ক্লিক অটো-রান পদ্ধতি (Developer-Friendly)

বট ক্লোন করার পর আপনাকে আর কোনো ম্যানুয়াল কনফিগারেশন করতে হবে না। মাত্র একটি স্ক্রিপ্ট রান করলেই সবকিছু অটোমেটিক সেটআপ হয়ে যাবে!

### ৩টি সহজ ধাপ:

#### ধাপ ১: ভিপিএস-এ কানেক্ট করুন এবং রিপোজিটরি ক্লোন করুন
```bash
# আপনার VPS-এ SSH দিয়ে ঢুকুন
ssh root@YOUR_VPS_IP

# রিপোজিটরি ক্লোন করে ফোল্ডারে ঢুকুন
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git telegram-poll-bot
cd telegram-poll-bot
```

#### ধাপ ২: .env ফাইলে আপনার বট টোকেন বসান
```bash
cp .env.example .env
nano .env
```
*(কিবোর্ডের অ্যারো দিয়ে নিচে গিয়ে `BOT_TOKEN` এবং `ADMIN_IDS` বসিয়ে দিন। এরপর সেভ করতে `Ctrl + O`, তারপর `Enter`, তারপর বের হতে `Ctrl + X` চাপুন)*

#### ধাপ ৩: ১-ক্লিক অটো-রান স্ক্রিপ্ট চালান
```bash
sudo bash setup_autorun.sh
```

---

### 🎉 ব্যস, কাজ শেষ! এখন আপনার বটের যা যা সুবিধা চালু থাকবে:
1. 🔄 **VPS রিবুট (Reboot) বা বন্ধ হয়ে চালু হলে:** বট নিজে নিজে ব্যাকগ্রাউন্ডে চালু হয়ে যাবে। কোনো কমান্ড দেওয়া লাগবে না।
2. 🛡️ **বট কোনো কারণে এরর/ক্র্যাশ করলে:** Systemd নিজে নিজে ৩ সেকেন্ডের মধ্যে বটকে রিস্টার্ট করে দেবে।
3. 💻 **টার্মিনাল বা পিসি বন্ধ করে দিলে:** ব্যাকগ্রাউন্ডে বট বিরতিহীনভাবে চলতে থাকবে।
4. 📂 **পাথ নির্ভুল:** আপনি ফোল্ডার যেখানেই রাখুন না কেন, স্ক্রিপ্ট নিজে নিজে সঠিক লোকেশন চিনে নিয়ে সার্ভিস তৈরি করবে।

---

## 🛠️ বটের দরকারি কমান্ডসমূহ (Cheat Sheet)

আপনার বট ঠিকঠাক চলছে কিনা বা দেখতে চাইলে নিচের সাধারণ কমান্ডগুলো ব্যবহার করুন:

| কি করতে চান | কি কমান্ড লিখবেন |
| :--- | :--- |
| **বট চলছে কিনা দেখতে (Status)** | `sudo systemctl status telegram-bot` |
| **লাইভ লগ দেখতে (Logs)** | `tail -f bot.log` |
| **বট রিস্টার্ট করতে (Restart)** | `sudo systemctl restart telegram-bot` |
| **বট বন্ধ করতে (Stop)** | `sudo systemctl stop telegram-bot` |
| **বট চালু করতে (Start)** | `sudo systemctl start telegram-bot` |

---

## 🔄 ভবিষ্যতে কোড আপডেট করার নিয়ম
লোকাল পিসি থেকে কোড গিটহাবে পুশ করার পর ভিপিএস-এ এক ক্লিকে আপডেট ও অটো-রিস্টার্ট করতে:
```bash
cd telegram-poll-bot
git pull
sudo systemctl restart telegram-bot
```

---

## 🐳 বিকল্প পদ্ধতি: Docker Compose দিয়ে রান করা (যদি ডকার পছন্দ করেন)

যদি আপনি Docker ব্যবহার করতে চান, তবে Docker-এও `restart: always` দেওয়া আছে, যাতে VPS রিবুট হলেও ডকার স্বয়ংক্রিয়ভাবে বট চালু করে:

```bash
# ১. ডকার কম্পোজ দিয়ে ব্যাকগ্রাউন্ডে রান করুন
docker compose up -d --build

# ২. লাইভ লগ দেখতে:
docker compose logs -f

# ৩. বন্ধ করতে:
docker compose down
```
